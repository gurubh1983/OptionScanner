# Root Procfile for Railway when deploying from repo root
# Set root directory to backend in Railway, or use: web: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
web: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
